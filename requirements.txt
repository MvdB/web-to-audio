# Core dependencies
beautifulsoup4>=4.12
lxml>=4.9
requests>=2.31
soundfile>=0.12
numpy>=1.24
pydub>=0.25
# Static ffmpeg binary (pip-only fallback if system ffmpeg is missing):
imageio-ffmpeg>=0.4

# Voxtral backend (HTTP client to vLLM-Omni server)
httpx>=0.27

# Qwen3-TTS backend (install separately, pulls in torch):
#   pip install -U qwen-tts
